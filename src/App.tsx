import * as React from 'react';
import TopPageContainer from 'container/TopPageContainer';

const App: React.FC = () => {
  return (
    <React.Fragment>
      <div style={{ margin: 40 }}>
        <TopPageContainer />
      </div>
    </React.Fragment>
  );
};

export default App;
